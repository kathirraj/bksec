$(document).on('startup', () => {
    /*
    //Check if the update notification pops up only for system manager role. If not then enable this.
    frappe.call({
        method: "bksec.bksec.disable_check_update.remove_update_notification"
    });
    */
});